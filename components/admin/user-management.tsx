'use client'

// Re-export from the original settings page component
// This allows both /settings/users and /admin/users to use the same component
export { default as UserManagement } from '@/app/accountant/settings/users/page'
